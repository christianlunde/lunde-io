import { about } from "./about";
import { destination } from "./destination";
import { journal } from "./journal";
import { project } from "./project";

export const schemaTypes = [about, destination, journal, project];
